module.exports = {
    // require: 'babel-register', // If you're using ES6 modules in your tests
    spec: 'tests/*.spec.js', // The file pattern for your test files
    timeout: 5000, // Maximum time in milliseconds for a test to run
    reporter: 'spec' // The test reporter to use (e.g., 'spec', 'dot', 'nyan')
  };
  